{
  "name": "browser-diagnostics",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.122.0"
  }
}
